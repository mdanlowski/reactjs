import React from 'react';
import ReactDOM from 'react-dom';
import App from "./App";

// Props are immutable
// State - component can retain it's data 
// Only class-based components can handle states

ReactDOM.render(<App />, document.getElementById('root'));